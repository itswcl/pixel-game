# Pixel Quiz Game (像素闖關問答遊戲)

這是一個使用 React Vite 開發的復古像素風格問答遊戲，並結合 Google Sheets 作為後端資料庫，實現題目管理與成績紀錄功能。

## ✨ 特色

- 🎨 **復古像素風格 (Pixel Art)**：精心設計的 2000 年代街機視覺體驗。
- 👾 **動態關主 Avatar**：每一題都會透過 DiceBear API 下載獨特的像素角色。
- 📊 **Google Sheets 整合**：無需架設傳統資料庫，直接用試算表管理題目與查看成績。
- 📱 **RWD 響應式設計**：支援桌機與手機遊玩。
- 🚀 **GitHub Actions 自動部署**：支援自動部署至 GitHub Pages。

---

## �️ 後端設置 (Google Sheets & Apps Script)

本專案使用 Google Sheets 作為後端。請依照以下步驟完成設置：

### 步驟 1：建立 Google Sheet

1. 前往 [Google Sheets](https://sheets.google.com) 建立一個新的試算表。
2. 將試算表名稱設為 `Pixel Quiz Database` (或自訂)。
3. **建立「題目」工作表**：
   - 將下方的工作表標籤重新命名為 `題目`。
   - 在第一列設定以下標題：
     `ID` | `Question` | `A` | `B` | `C` | `D` | `Answer`
4. **建立「回答」工作表**：
   - 新增一個工作表，命名為 `回答`。
   - 在第一列設定以下標題：
     `ID` | `PlayCount` | `TotalScore` | `MaxScore` | `FirstPassScore` | `AttemptsToPass` | `LastPlayed`

### 步驟 2：部署 Google Apps Script

1. 在試算表中，點擊上方選單的 **擴充功能 (Extensions)** > **Apps Script**。
2. 將專案根目錄下的 `google_apps_script.js` 檔案內容完整複製。
3. 貼上覆蓋掉 Apps Script 編輯器中的預設程式碼 (`function myFunction() {...}`)。
4. 點擊上方的 **部署 (Deploy)** > **新增部署 (New deployment)**。
5. 點擊齒輪圖示 ⚙️ > 選取 **網頁應用程式 (Web app)**。
6. 設定如下：
   - **說明 (Description)**: Pixel Game Backend
   - **執行身分 (Execute as)**: **我 (Me)**
   - **誰可以存取 (Who has access)**: **所有人 (Anyone)** ⚠️ _這一點非常重要，否則遊戲無法讀取資料_
7. 點擊 **部署 (Deploy)**。
8. 複製產生的 **網頁應用程式網址 (Web App URL)**。

---

## 💻 前端設置與執行

### 1. 安裝與設定

```bash
# 安裝依賴
npm install

# 複製環境變數範例檔
cp .env.example .env
```

### 2. 配置環境變數

打開 `.env` 檔案，將上一步取得的 Web App URL 填入：

```env
VITE_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/你的部署ID/exec
VITE_PASS_THRESHOLD=3  # 通過門檻 (答對幾題及格)
VITE_QUESTION_COUNT=5  # 每次遊戲的題目數
```

### 3. 本地開發

```bash
npm run dev
```

打開瀏覽器訪問顯示的 Local URL (通常是 http://localhost:5173)。

---

## 📝 範例題庫：生成式 AI 基礎知識

您可以直接複製以下表格內容到 Google Sheet 的**「題目」**工作表中進行測試。

| ID  | Question                                                                | A                                  | B                           | C                             | D                        | Answer |
| :-- | :---------------------------------------------------------------------- | :--------------------------------- | :-------------------------- | :---------------------------- | :----------------------- | :----- |
| 1   | 下列何者是「生成式 AI」的主要特徵？                                     | 只能分析既有數據                   | 只會做數學運算              | 能創造新的內容 (如文字、圖片) | 只能搜尋網路資料         | C      |
| 2   | ChatGPT 背後使用的核心技術架構是什麼？                                  | CNN (卷積神經網路)                 | Transformer (變換器)        | RNN (循環神經網路)            | GAN (生成對抗網路)       | B      |
| 3   | 在 Prompt Engineering (提示工程) 中，「幻覺 (Hallucination)」是指什麼？ | AI 拒絕回答問題                    | AI 生成看似合理但錯誤的資訊 | AI 運算速度變慢               | AI 能夠預測未來          | B      |
| 4   | Midjourney 和 Stable Diffusion 主要用於生成什麼類型的內容？             | 程式碼                             | 圖片                        | 音樂                          | excel 表格               | B      |
| 5   | 大型語言模型 (LLM) 中的「Token」通常是用來計算什麼的單位？              | 運算時間                           | 電力消耗                    | 文本數據量 (輸入/輸出)        | 模型價格                 | C      |
| 6   | 也就是所謂的「少樣本學習 (Few-shot Learning)」是指？                    | 給模型看數千張照片                 | 不給模型任何範例            | 給模型少量範例讓它學習模式    | 重新訓練整個模型         | C      |
| 7   | 下列哪個不是常見的生成式 AI 應用？                                      | 寫電子郵件草稿                     | 自動修復實體硬碟壞軌        | 生成行銷文案                  | 修改程式碼 Bug           | B      |
| 8   | 關於 RAG (Retrieval-Augmented Generation)，下列敘述何者正確？           | 讓 AI 結合外部知識庫來回答問題     | 是一種新的畫圖技術          | 只能用在語音辨識              | 會讓 AI 忘記所有訓練資料 | A      |
| 9   | 訓練 GPT-4 這類模型通常需要哪種硬體資源？                               | 普通家用 CPU                       | 大量的 GPU (圖形處理器)     | 硬碟空間即可                  | 手機晶片                 | B      |
| 10  | 為了讓生成式 AI 的回答更精準，通常會建議？                              | 提供明確的指令與背景資訊 (Context) | 只給一個關鍵字              | 用完全看不懂的語言提問        | 不要給任何限制           | A      |

---

## 🚀 部署 (GitHub Pages)

本專案已設定 GitHub Actions，可自動部署至 GitHub Pages。

1. 將代碼 Push 到 GitHub。
2. 在 GitHub Repo 設定頁面：**Settings** > **Secrets and variables** > **Actions**。
3. 新增 Secret：`VITE_GOOGLE_SCRIPT_URL`，填入你的 GAS Web App URL。
4. 前往 **Settings** > **Pages**，將 Source 設為 **GitHub Actions**。
5. 之後每次 Push 到 `main` 分支，都會自動觸發部署。

---

## 📂 專案結構

```
src/
  ├── components/    # 共用元件 (PixelCard 等)
  ├── pages/         # 頁面 (Start, Game, Result)
  ├── services/      # API 服務 (與 Google Apps Script溝通)
  ├── index.css      # 全域樣式與像素風格變數
  └── App.jsx        # 主應用程式邏輯
```
